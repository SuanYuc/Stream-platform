# 设计规范

本项目当前采用 Quickish 风格的深色产品界面方案。新增页面、弹窗、子界面和组件时，先读取本文件，再复用 `nsy_broadcasting_platform/ui/theme.py` 中的主题变量。

## 配色

主背景色为 `#0D0D0F`，用于整页底色和最外层容器。次背景色为 `#111214`，用于分栏背景、侧栏和次级区域。面板色为 `#141416`，用于卡片、列表、对话框主体。抬升面板色为 `#1C1C1F`，用于 hover、激活态和更重要的内容卡片。输入背景色为 `#101113`，用于表单控件和文本输入区域。

主强调色为 `#4AC777`，用于主按钮、选中态、当前状态、选区、关键操作。主强调 hover 为 `#5FD78E`，主强调深色为 `#22864A`。AI 辅助色为 `#5C7DB7`，AI hover 为 `#7E9DD7`，只用于 AI 功能区、AI 标签和 AI 相关主按钮。

文字主色为 `#FBFBF9`，文字次色为 `#8A8A8A`，文字弱色为 `#686B70`。危险色为 `#A35655`，危险深色为 `#3E1E1E`。边框主色为 `#2B2D31`，弱边框为 `#1F2126`。

状态颜色规则如下：

- 运行中 / 成功：以绿色系呈现，优先使用主强调色或其深色变体。
- 选中 / 焦点：优先使用主强调色细边框。
- AI 区域：优先使用蓝色系，不与全局主强调混用。
- 危险操作：只使用深红色，不使用高饱和亮红。

## 字体

标题字体优先使用 `Hanken Grotesk`，中文回退到 `Microsoft YaHei UI`、`PingFang SC`、`Segoe UI`。正文也使用同一无衬线栈，保持统一的产品感。

等宽字体优先使用 `JetBrains Mono`，回退到 `Cascadia Mono`、`Consolas`、`SFMono-Regular`。URL、状态值、控制台文本、路径、时间戳、标签值优先使用等宽字体。

建议字号层级如下：

- 页面/窗口标题：18px - 20px，字重 800。
- 一级标题：24px - 28px，字重 700 - 800。
- 二级标题：18px - 22px，字重 700 - 800。
- 正文：13px - 15px，字重 400 - 600。
- 辅助说明：11px - 12px，字重 400 - 500。
- 状态芯片/标签：11px - 13px，字重 700 - 800，优先等宽字体。

建议行高如下：

- 标题行高：1.05 - 1.15。
- 正文行高：1.5 - 1.7。
- 说明与辅助文本行高：1.4 - 1.6。

字距规则：

- 标题可使用轻微负字距，保持紧凑。
- 状态字、标签字和按钮字可使用轻微正字距，增强“控制台感”。

## 间距

页面外边距建议保持在 10px - 12px。模块之间建议保持 16px - 24px。大区块之间建议保持 28px - 40px。卡片内部边距建议保持 14px - 18px。组件之间的细间距建议保持 6px - 8px。

圆角规则：

- 小控件：8px - 12px。
- 标准卡片：18px - 20px。
- 大卡片 / 对话框：20px - 26px。
- Pill 按钮和状态芯片：999px。

布局节奏：

- 先放顶部标题、状态和关键操作。
- 中间放主工作区和核心画面。
- 下方放参数、列表和辅助操作。
- 同一屏中尽量减少重复说明文字，优先用状态、标签和分区标题表达信息。

## 组件样式

按钮统一使用扁平、克制、圆角较大的样式。主按钮使用绿色填充、深色文字。次按钮使用深色底和细边框。危险按钮使用深红底。工具按钮使用更小的 pill 样式，保持低干扰。按钮 hover 时只做轻微提亮和边框变化，不做夸张动画。

卡片统一使用深色底、1px 细边框、18px - 20px 圆角。默认不使用大面积软阴影；如果需要层级感，优先使用硬边、轻偏移的 shadow 或通过更亮的面板色来表现。选中态使用绿色细边框，AI 卡片使用蓝色细边框。

导航栏和状态栏统一使用深色 rail 风格，信息以胶囊标签显示。当前场景、推流状态、录制状态、设备状态等优先使用状态芯片，不使用长句堆叠。

输入框、下拉框和数值框统一为深色底、细边框、14px 左右圆角。聚焦时改为主强调色边框。列表组件统一为深色面板，选中态用主强调色背景或边框，hover 时只做轻微提亮。

AI 功能区必须和普通功能区有视觉区分，但仍保持克制。只允许使用蓝色系作为局部强调，不要把蓝色扩散到全局。

音频与调音台组件优先使用等宽字体、窄间距和电平条表达状态。声纹、电平、通道条要简洁，不使用娱乐化图案。

## 布局参考

整体页面节奏参考 quickish 的排版方式：顶部是轻量导航/状态区，中间是主工作区或主信息区，下方是功能分区和卡片块。页面要有明显的主次层级，不要所有模块都同等突出。每个页面只保留一处最强视觉焦点，其余区域都作为辅助工作面板存在。

## 约束

不要使用霓虹风、强渐变、大面积模糊阴影、纯黑纯白、过高饱和红蓝。不要让控件彼此抢视觉焦点。所有新页面必须优先复用 `theme.py` 的主题变量，避免在业务代码里硬编码颜色和字体。

